import Groq from 'groq-sdk'

const FIELD_GUIDES: Record<string, string> = {
  name: `Это НАЗВАНИЕ товара. Сделай короткое, ёмкое, конкретное название (до ~70 символов). Без цен и длинных описаний — только суть товара.`,
  shortDesc: `Это КРАТКОЕ ОПИСАНИЕ для карточки товара. Одно-два ёмких предложения, передающих главную ценность. Без полного перечня характеристик и цен.`,
  description: `Это ПОЛНОЕ ОПИСАНИЕ товара. Преврати сырой текст клиента в структурированную коммерческую презентацию:
- Начни с короткого вводного абзаца о товаре.
- Затем логично сгруппируй характеристики, материалы и размеры.
- Если есть варианты комплектации и цены — оформи их понятным списком (каждый вариант с ценой на отдельной строке).
- Если есть преимущества/отличия от конкурентов — вынеси их отдельным блоком.
- Используй абзацы и списки (можно простые переносы строк и дефисы для списков).
Пиши как профессиональная продающая презентация, но СПОКОЙНО и по делу.`,
}

export default defineEventHandler(async (event) => {
  const config = useRuntimeConfig()
  const { text, field } = await readBody(event)

  if (!text) throw createError({ statusCode: 400, message: 'text is required' })

  const groq = new Groq({ apiKey: config.groqApiKey })

  const fieldGuide = FIELD_GUIDES[field as string] ?? FIELD_GUIDES.description

  const completion = await groq.chat.completions.create({
    model: 'llama-3.3-70b-versatile',
    messages: [
      {
        role: 'system',
        content: `Ты — редактор и копирайтер B2B-сайта украинского производителя спортивных тренажёров OgnevSport.
Клиент пишет тексты быстро, с ошибками, опечатками и без структуры. Твоя работа — превратить это в чистый, грамотный, продающий текст.

ЧТО ДЕЛАТЬ:
1. Определи язык исходного текста (ua/ru/en) и пиши ответ НА ТОМ ЖЕ языке.
2. Исправь все орфографические, пунктуационные и грамматические ошибки.
3. Приведи текст в порядок: структура, логичный порядок, читаемость.
4. ${fieldGuide}

КРИТИЧЕСКИ ВАЖНО — ТОЛЬКО ФАКТЫ КЛИЕНТА:
- НИКОГДА не выдумывай характеристики, цифры, материалы, гарантии, сроки, преимущества.
- Используй ТОЛЬКО ту информацию, которую дал клиент. Все цены, размеры (например «труба 50×50», «диаметр 48 мм»), материалы — переноси точно как есть.
- Если клиент чего-то не указал — не добавляй это от себя.
- Можно перефразировать и структурировать, но нельзя добавлять новые факты.
- Не добавляй пожелания вроде «мира и хороших покупок» — оставляй только товарную суть.
- ЗАПРЕЩЕНЫ обобщённые рекламные фразы-заполнители, если клиент их не писал: «высокое качество», «индивидуальный подход», «наши преимущества», «гарантия качества», «опытные специалисты» и т.п. Не добавляй заключительный абзац «от себя».
- Перепроверь себя: каждое предложение в твоём ответе должно опираться на конкретный факт из текста клиента.

ВАЛЮТА И ЕДИНИЦЫ:
- Это УКРАИНСКИЙ производитель. Все цены — в ГРИВНАХ (грн). НИКОГДА не пиши «рублей»/«рубли»/«₽» — всегда «грн».
- Если клиент написал «9тыс», «11тыс» — это «9 000 грн», «11 000 грн». Сохраняй валюту гривна, даже если клиент указал её только один раз или не указал явно.
- Не меняй и не конвертируй числа, размеры и единицы измерения (мм, см, кг, тыс).

ТЕРМИНЫ И НАЗВАНИЯ МАТЕРИАЛОВ (СТРОГО):
- Названия материалов, тканей, комплектующих и технические термины переноси ДОСЛОВНО, как написал клиент. Только исправляй явные орфографические ошибки в них.
- Пример: «ПВХ ткань» всегда остаётся «ПВХ ткань»/«ПВХ тканью» — НИКОГДА не превращай в «ПВХ тьму», «ПВХ плёнку» или другое слово.
- Запрещено заменять одно слово на похожее по звучанию. Если сомневаешься в слове — оставь точно как у клиента.
- Профильная труба, барашки, обрезиненные ножки, эластичные материалы, металлические основания — все такие термины сохраняй точно.

Верни ТОЛЬКО валидный JSON без markdown-блоков:
{"correctedText": "...", "detectedLang": "ua|ru|en"}`,
      },
      { role: 'user', content: text },
    ],
    temperature: 0.2,
    max_tokens: 2048,
    response_format: { type: 'json_object' },
  })

  const raw = completion.choices[0].message.content ?? '{}'
  const result = JSON.parse(raw)

  return {
    correctedText: sanitizeTerms(result.correctedText ?? text),
    detectedLang: result.detectedLang ?? 'ua',
  }
})
